# Lab 5 – Create Database, Create Table and Insert Data

## Description
This lab demonstrates creating a database, creating a table, and inserting records into a MySQL database.

## Tasks Completed
- Created database `bookshop` (if it didn’t already exist)
- Selected the `bookshop` database
- Created the `customers` table with columns:
  - `customerID` INT
  - `customerName` VARCHAR(50)
  - `customerAddress` VARCHAR(255)
- Inserted first customer record (Jack)
- Optional: Inserted second customer record (James)
- Verified table contents with `SELECT * FROM customers;`

## Status
Lab completed successfully.  
**Grade:** 100%