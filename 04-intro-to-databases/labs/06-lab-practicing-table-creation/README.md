# Lab 6 – Practicing Table Creation

## Description
This lab focuses on practicing how to create a database and build tables from scratch in MySQL.

## Tasks Completed
- Created database `football_club` (if it didn’t already exist)
- Selected the `football_club` database
- Created the `players` table with columns:
  - `playerID` INT
  - `playerName` VARCHAR(50)
  - `age` INT
- Verified table creation using `SHOW TABLES;`

## Optional Task
- Created the `games` table with:
  - `gameID` INT
  - `gameDate` DATE
  - `score` INT

## Status
Lab completed successfully.  
**Grade:** 100%